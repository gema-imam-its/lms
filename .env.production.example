# Copy this to `.env.production` ON THE VPS ONLY (never commit that file,
# never put it in this repo's build context beyond this template).
# docker-compose.prod.yml reads it via `env_file:` at container start.

NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
IOT_SECRET_KEY=
